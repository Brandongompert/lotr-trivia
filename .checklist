Core Setup
✅ Add Tailwind CSS config and base styles
✅ Add Framer Motion animations
✅ Define trivia TypeScript types
✅ Create sample trivia JSON

UI Components
✅ TriviaCard (animated flip or slide)
✅ TriviaDeck (question progression logic)
✅ ProgressBar (visual progress indicator)
✅ Disclaimer (unofficial fan site notice)

Functionality
✅ Load trivia from static JSON
✅ Handle next / previous navigation
✅ Handle answer reveal
✅ Keyboard navigation support
✅ Optional localStorage progress persistence

Quality
✅ Clean folder structure
✅ No unnecessary dependencies
✅ No backend assumptions
✅ TypeScript compiles with no errors
⚠️  App runs with npm run dev (requires Node 20.19+)
⚠️  App builds with npm run build (requires Node 20.19+)

Notes:
- All code is implemented and TypeScript-validated
- Node.js version 20.19+ or 22.12+ is required to run/build
- Current Node version detected: 19.7.0 (needs upgrade)
